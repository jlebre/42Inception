version: "3.8"

services:
  nginx:
    build: ./requirements/nginx/.
    restart: always
    ports:
      - 443:443
    volumes:
      - wp:/var/www/html
    networks:
      - project

networks:
  project:
    driver: bridge

volumes:
  wp:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: /home/jlebre/data/wp
